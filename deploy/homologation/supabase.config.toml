# Runtime exclusivo de homologação. Copiado para diretório root-only pelo runbook.
project_id = "descomplica-homologation"

[api]
enabled = true
port = 55321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[api.tls]
enabled = false

[db]
port = 55322
shadow_port = 55320
health_timeout = "2m"
major_version = 17

[db.pooler]
enabled = false
port = 55329
pool_mode = "transaction"
default_pool_size = 10
max_client_conn = 50

[db.migrations]
enabled = true
schema_paths = []

[db.seed]
enabled = false
sql_paths = []

[realtime]
enabled = false

[studio]
enabled = false
port = 55323
api_url = "http://127.0.0.1"

[local_smtp]
enabled = true
port = 55324

[storage]
enabled = false
file_size_limit = "1MiB"

[storage.s3_protocol]
enabled = false

[auth]
enabled = true
site_url = "https://homolog.descomplicapro.com.br"
additional_redirect_urls = ["https://homolog.descomplicapro.com.br/auth/callback"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = false
enable_anonymous_sign_ins = false
enable_manual_linking = false
minimum_password_length = 12
password_requirements = "lower_upper_letters_digits_symbols"

[auth.rate_limit]
# A matriz hospedada compara conta inexistente e conta QA dedicada. O Mailpit
# isolado não entrega mensagens para fora da VPS; a folga permite repetir o
# gate controlado sem desabilitar o limite.
email_sent = 10
sms_sent = 1
anonymous_users = 1
token_refresh = 60
sign_in_sign_ups = 120
token_verifications = 300
web3 = 1

[auth.email]
# Mantém login por e-mail ativo; [auth].enable_signup=false continua bloqueando
# criação pública de contas no Auth isolado.
enable_signup = true
double_confirm_changes = true
enable_confirmations = false
secure_password_change = true
# Ambiente sintético protegido por Basic Auth: permite repetir o smoke sem
# esperar um minuto, mantendo o limite global de dez mensagens por hora.
max_frequency = "1s"
otp_length = 6
otp_expiry = 3600

[auth.email.template.recovery]
subject = "Redefina sua senha"
content_path = "./supabase/templates/recovery.html"

[auth.sms]
enable_signup = false
enable_confirmations = false
template = "Your code is {{ .Code }}"
max_frequency = "1m"

[auth.mfa]
max_enrolled_factors = 2

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "1m"

[auth.oauth_server]
enabled = false
authorization_url_path = "/oauth/consent"
allow_dynamic_registration = false

[edge_runtime]
enabled = false
policy = "oneshot"
inspector_port = 55883
deno_version = 2

[analytics]
enabled = false
port = 55327
backend = "postgres"
